#include<bits/stdc++.h>
using namespace std;
typedef long long int lli;

int main()
{
   lli test;
   cin>>test;
   for(lli e=0;e<test;e++)
   {
       lli a,b;
       cin>>a>>b;
       if(a>b)
       {
           cout<<b-a<<endl;
       }
       else
       {
           // a<b
           lli pos=b-a;
           lli pos2=(3*a)-b;
           if(b%3==0)
           {
               pos3=(b/3)-a;
           }
           else
           {
               pos3=
           }
       }
   }

    return 0;
}

