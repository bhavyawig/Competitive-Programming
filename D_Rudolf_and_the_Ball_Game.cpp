#include<bits/stdc++.h>
using namespace std;
typedef long long int lli;

int main()
{
    lli t;
    cin>>t;
    for(lli e=0;e<t;e++)
    {
        lli n,m,x;
        cin>>n>>m>>x;
        vec<pair<lli,char>> vec;
        for(lli i=0;i<n;i++)
        {
            lli pp; char tt;
            cin>>pp>>tt;
            vec.push_back({pp,tt});
        }
        vector<vector<int>> clock(n+1, vector<int> (n+1,0));

        for(lli i=1;i<=n;i++)
        {
            for(lli j=1;j<=n;j++)
            {
                
            }
        }
    }
}