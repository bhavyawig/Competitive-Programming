#include<bits/stdc++.h>
using namespace std;
typedef long long int lli;

int main()
{
   string  s1,s2;
   cin>>s1>>s2;
   if(s1==s2)
   {
       cout<<"Equal"<<endl;
   }
   else if(s1>s2)
   {
       cout<<"s1 is larger"<<endl;
   }
   else
   {
       cout<<"S2 is larger"<<endl;
   }

    return 0;
}

