#include<bits/stdc++.h>
using namespace std;
typedef long long int lli;

int main()
{
   lli a,b;
   cout<<"Enter two numbers"<<endl;
   cin>>a>>b;
   // Write a program to swap two numbers
   // time complexity is O(1)
   //swap(a,b);
   swap(a,b);
  cout<<"Value of a is :"<<a<<endl;
  cout<<"Value of b is :"<<b<<endl;


   // GCD of two numbers
   //  GCD : Greatest common divisor
   //  The greatest common divisor of two integers is the largest positive integer that divides each of the integers.
   // time complexity is O(log n)
   //cout<<__gcd(24,18)<<endl;
   //cout<<__gcd(10,6)<<endl;
   //syna:__gcd(a,b);

   // To calculate gcd of three numbers
   //cout<<__gcd(10,__gcd(6,4))<<endl;
    return 0;
}

